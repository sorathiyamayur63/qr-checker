# QR Threat Intelligence Platform

## Setup Instructions

### Local Development
1. Install dependencies: `pnpm install`
2. Start development servers: `pnpm dev`

### Docker Setup
To run everything via Docker (Web, API, Postgres, Redis):
```bash
docker compose up
```
